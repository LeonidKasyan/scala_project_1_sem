database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      # serverName = postgres
      # portNumber = 5432
      # databaseName = scala-postgres
      # user = postgres
      # password = 1234
      serverName = ${PGHOST}
      portNumber = ${PGPORT}
      databaseName = ${PGDATABASE}
      user = ${PGUSER}
      password = ${PGPASSWORD}
    }
  }
}

port = 8080